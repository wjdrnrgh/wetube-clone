extends layout.pug
include mixins/video

block content
    div.content__wrap
        div.all__videos
            each video in videos
                +video(video)
                +video(video)
                +video(video)
            else
                h1.empty__message Nothing Found.